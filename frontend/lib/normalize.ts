function num(value: unknown, fallback = 0): number { if (value === null || value === undefined || value === '') return fallback const n = Number(value) return Number.isFinite(n) ? n : fallback } function numOrNull(value: unknown): number | null { if (value === null || value === undefined || value === '') return null const n = Number(value) return Number.isFinite(n) ? n : null } function normalizeField<T extends Record<string, unknown>>(raw: T, field: keyof T, fallback: number | null = 0): T { const value = raw[field] if (value === null || value === undefined || value === '') return { ...raw, [field]: fallback } const n = Number(value) return Number.isFinite(n) ? { ...raw, [field]: n } : { ...raw, [field]: fallback } } export function normalizeSubscription<T extends Record<string, unknown>>(raw: T): T { return { ...raw, amount: num(raw.amount), confidence: numOrNull(raw.confidence), } as T } export function normalizeRec<T extends Record<string, unknown>>(raw: T): T { return { ...raw, amount: num(raw.amount), confidence: num(raw.confidence), } as T } export function normalizeAction<T extends Record<string, unknown>>(raw: T): T { return { ...raw, amount: num(raw.amount), } as T }